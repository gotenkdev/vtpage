.DS_Store
*.log
.vscode/

# Backend nằm ở repo riêng (PRIVATE), không bao giờ commit vào repo này:
# repo này công khai và GitHub Pages phục vụ toàn bộ thư mục gốc.
backend/
.env
.env.*
captures/

# Impeccable: ảnh chụp và cache tạm của phiên thiết kế
.impeccable/review/
.impeccable/questions/
.impeccable/build/
.impeccable/surfaces/
